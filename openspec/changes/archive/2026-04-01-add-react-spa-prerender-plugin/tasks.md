## 1. 仓库基线与测试脚手架

- [x] 1.1 清理当前组件模板导出、示例与测试基线，确定插件仓库的目录结构与公开入口
- [x] 1.2 使用 Vitest 先补齐配置加载与非法配置回退的失败用例
- [x] 1.3 使用 Vitest 补齐路由模式、单页模式与页面级失败回退的核心行为用例

## 2. 配置与类型接口

- [x] 2.1 实现 `defineReactSsgConfig` 及其对象/函数两种配置形式的类型定义
- [x] 2.2 实现 `react-ssg.config.ts` 的加载、解析与校验逻辑
- [x] 2.3 实现缺少配置文件或配置非法时的非致命提示与全局 CSR 回退逻辑

## 3. 预渲染核心流程

- [x] 3.1 实现路由模式下的静态路径发现与 `paths` 合并逻辑
- [x] 3.2 实现 `browser` history 的多路径 HTML 预渲染输出
- [x] 3.3 实现 `hash` history 的默认首屏预渲染输出
- [x] 3.4 实现单页 `app` 模式的根路径预渲染输出
- [x] 3.5 实现单页面失败记录、按目标跳过与 CSR 回退逻辑

## 4. 集成验证与文档

- [x] 4.1 更新 playground，分别提供路由模式与单页模式的最小示例
- [x] 4.2 更新 README，说明配置文件约定、支持范围、失败回退语义与当前非目标
- [x] 4.3 运行并修复类型检查、测试与构建，确保变更满足 TDD 与发布基线
