## 1. 规格与测试基线

- [x] 1.1 为包元数据中的 React peer 兼容范围补充 Vitest 回归测试

## 2. 包元数据修正

- [x] 2.1 放宽 `package.json` 中的 `react` 与 `react-dom` peerDependencies 到 `>=16.8`
- [x] 2.2 保持 `react-router` peer 范围不变，并验证测试通过
