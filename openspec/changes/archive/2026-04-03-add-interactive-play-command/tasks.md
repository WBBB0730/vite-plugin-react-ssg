## 1. OpenSpec 与测试基线

- [x] 1.1 补齐 proposal、design、spec delta，明确 `pnpm play` 的交互式补全边界
- [x] 1.2 使用 Vitest 先补齐 `pnpm play` 参数解析、缺失参数交互补全和命令拼装的失败/正向用例

## 2. CLI 实现

- [x] 2.1 引入轻量交互式终端依赖，并在 `scripts/play.ts` 中实现缺失参数时的交互式补全
- [x] 2.2 保持完整参数直通、非法参数报错与 `-- <vite-args>` 透传行为
- [x] 2.3 为交互取消与无本地 `dist` 的前置校验补齐稳定退出路径

## 3. 验证与收尾

- [x] 3.1 运行相关 Vitest 用例并修正实现
- [x] 3.2 运行脚本相关类型检查，确认新依赖与 `scripts/` 类型边界正常
